原生应用是由一个或多个关联的计算组件（Workloads）、内部路由（Services）等 Kubernetes 原生资源构成的整体业务应用。支持通过 UI 编辑、YAML 编排，以及模板等方式创建，可运行在开发、测试或生产环境中。

* 创建原生应用

支持表单创建、模板部署、向导部署和YAML一键部署等方式。

* 采用表单创建通过可视化 UI 表单配置原生应用的基本信息及资源。

![图形用户界面, 应用程序, Teams  描述已自动生成](data:image/png;base64...)

表单创建

* 采用模板部署会跳转至应用商店界面，应用模板（Helm Chart）为一个描述 Kubernetes 相关资源的文件集合，通常用于封装应用程序并进行应用分发。获取模板后，可基于应用模板部署原生应用。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

模板部署

* 采用向导方式将通过界面向导配置原生应用的基本信息及资源。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

向导创建

* YAML 一键创建方式可以更为灵活地配置原生应用的基本信息及资源。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

YAML一键创建

* 原生应用管理

应用管理界面支持对已创建的原生应用进行监控管理。

* 支持应用联邦化：将联邦命名空间下的普通应用，联邦化升级为联邦应用。联邦化的过程中，会在联邦集群其他的成员的同名联邦命名空间下创建同名的联邦应用，实现跨集群部署应用。
* 随应用创建的资源默认就属于应用，无需手动导入。在应用之外创建了计算组件、网络、存储及配置等资源，建议将业务所需资源都导入应用，以便在应用层面统一管理资源。
* 支持控制应用的启动或暂停，也支持更新或删除原生应用。
* 支持通过EXEC进入容器，在 Web 控制台窗口执行命令行操作。
* 支持横向扩增或减少应用中 Deployment、 StatefulSet 类型计算组件的容器组。
* 通过平台界面更新应用后，会自动生成历史版本记录。对于非界面操作引发的应用更新，例如通过调用 API 更新应用后，可手从创建版本快照记录变更。包括手动和自动扩缩容。
* 支持应用分发，将原生应用导出为应用模板（Chart），以在不同环境或不同命名空间中再次创建原生应用，实现业务的规范化分发和管理。
* 支持使用 Prometheus 获取原生应用的监控数据，支持以可视化图表的方式展示近 7 天内的资源监控数据。

![电脑软件截图  描述已自动生成](data:image/png;base64...)

原生应用管理



平台支持将已创建的应用或应用的版本快照导出为应用模板（Helm Chart），支持将应用模板分发到模板仓库或生成并下载 Helm Chart软件包，用于业务的规范化分发和管理。

模板应用是通过helm chart资源快速部署的定制化应用实例。平台支持用户查看模板应用的部署情况、将模板应用升级为原生应用或删除模板应用。

如果用户对模板应用有更新需求，可以升级成原生应用进行更新等操作。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

模板应用管理界面



组件应用指基于应用组件（operator backed）部署的应用，支持通过平台可视化界面对组件应用进行管理，包括查看状态、执行更新或删除等操作。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

组件应用管理界面



支持在联邦集群的所有成员集群下，跨集群部署和管理相同的应用，实现“两地三中心”容灾备份解决方案。

可在联邦集群的控制集群下的联邦命名空间中创建、管理联邦应用。

![图形用户界面, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

联邦应用管理界面

创建联邦应用后，会在联邦集群的所有成员集群的联邦命名空间下，创建一个同名的应用以及应用下的其他联邦资源，例如：部署、配置字典、内部路由、有状态副本集等。

![图形用户界面, 应用程序, Teams  描述已自动生成](data:image/png;base64...)

创建联邦应用





部署（Deployment）是Kubernetes的一种工作负载控制器（Controllers），支持通过创建一个Deployment来运行一个程序。也可以在应用中创建多个Deployment作为应用下的工作负载来为无状态应用提供特定的服务。

Deployment为ReplicaSet（副本控制器）和Pod（容器组）提供了声明式的更新。只需要在Deployment 中描述了对 Replicas（Pods 数量）和Container（容器）的期望状态，Deployment控制器会将Pod和ReplicaSet的实际状态改变至期望状态。

Deployment 的功能特点如下：

* 可以通过创建一个Deployment来启动一个ReplicaSet。ReplicaSet会根据设定的副本数创建Pods，并检查Pods的启动状态。
* 可以通过更新Deployment的PodTemplateSpec字段来声明Pods的新状态。声明Pods的新状态后，会创建一个新的ReplicaSet，Deployment会以可控的速率将Pods从旧的ReplicaSet迁移至新的ReplicaSet。同时，ReplicaSet会更新Deployment的版本。
* 如果当前Deployment的状态不稳定，将回滚至更早版本的Deployment。每次回滚将会更新Deployment的版本。
* 可以通过扩容Deployment来满足更高的负载。
* 可以通过暂停Deployment来应用PodTemplateSpec的多个修复，然后重新上线。
* 可以将Deployment的状态作为判断上线是否停滞的指标。
* 可以通过Deployment清理不再需要的旧的ReplicaSet。

![图形用户界面, 应用程序, Teams  描述已自动生成](data:image/png;base64...)

部署管理界面

* 创建部署

平台支持两种方式创建部署，包括YAML部署和可视化UI表单部署。

![图形用户界面, 应用程序, Teams  描述已自动生成](data:image/png;base64...)

YAML创建部署

采用可视化部署需要先拉取镜像，在创建部署过程中支持切换至YAML界面。

![图形用户界面, 应用程序  描述已自动生成](data:image/png;base64...)

拉取镜像部署

![图形用户界面, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

可视化部署

* 部署管理

支持对已部署的组件进行监控和管理：

* 支持更新、删除、启动、停止、重启等操作。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

部署详情信息展示

* 支持配置自动扩缩容：将对于业务量常有变动的应用，在某个已知或未知的业务变化（例如月末的账务结算高峰）来临前，提前进行扩容或缩容设置。当满足所设条件后，平台将自动扩缩应用中容器组个数，以满足业务需要。

![图形用户界面, 应用程序, Teams  描述已自动生成](data:image/png;base64...)

更新自动伸缩

* 支持查看平台上的计算组件近7天内的资源监控数据，包括计算组件下容器组统计数据以及CPU、内存等资源的用量趋势和排行统计数据。

![图形用户界面, 应用程序  描述已自动生成](data:image/png;base64...)

查看资源监控数据