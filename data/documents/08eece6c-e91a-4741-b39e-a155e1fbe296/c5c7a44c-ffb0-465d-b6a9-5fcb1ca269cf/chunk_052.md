* 修订日期：20xx-xx-xx
* 处置时间：约20分钟
* 操作步骤：

1. 根据监控告警信息判定异常主机；
2. 登陆异常主机，systemctl status kubelet查看kubelet状态
3. 执行systemctl restart kubelet启动kubelet服务
4. 启动失败需要查看kubelet日志（journalctl -fu kubelet），如果值班人员无法解决，可联系运维人员协助排查。



* 修订日期：20xx-xx-xx
* 处置时间：约20分钟
* 操作步骤：

1. 根据监控告警信息判定异常主机；
2. 登陆异常主机集群的master（主机设备中信息），kubectl get node查看NotReady状态主机；
3. 登陆NotReady主机；

* 情况一：docker ps查看容器是否全部Up状态，并且没有Restarting容器，否则重启docker；
* 情况二：systemctl status kubelet状态是否正常。

上述步骤之后依然未能恢复节点，可联系运维人员协助排查。



* 修订日期：20xx-xx-xx
* 处置时间：约20分钟
* 操作步骤：

1. 根据监控告警信息判定异常主机；
2. 登陆异常主机docker ps | grep etcd查看etcd容器的状态

* 情况一：etcd容器状态为Exited，dockerrestartetcd重启容器。
* 情况二etcd状态为dead，

执行echo“fs.may\_detach\_mounts=1”>>/etc/sysctl.conf，删除etcd容器docker rm etcd，docker ps | grep etcd知道etcd正常运行即可。



* 修订日期：20xx-xx-xx
* 处置时间：约20分钟
* 操作步骤：

1. 根据监控告警信息判定异常主机；
2. 登陆异常主机systemctl status mysql查看mysql服务状态
3. 重启mysql，systemctl restart mysqld，netstat -antlp |grep 3316有监听3316端口即可





* 修订日期：20xx-xx-xx
* 处置时间：约30分钟
* 操作步骤：

1. 部署应用失败
2. 登录控制台（xxx.xxx.xxx.xxx），进入镜像仓库列表，查找镜像，看镜像仓库是否存在，权限是否正确。
3. 登陆pull images失败的主机，在主机节点上编辑/etc/docker/daemon.json这个配置文件，在"insecure-registries"这增加镜像仓库的地址，格式参考下面的例子
   "insecure-registries":["10.1.0.23:5000","172.18.0.4:60080"]，重启docker daemon，执行systemctl daemon-reload，systemctl restart docker



* 修订日期：20xx-xx-xx
* 处置时间：约30分钟
* 操作步骤：

1. 开放管理平台上alb的36010端口
2. 进入hbase容器，执行sh /setup-opentsdb.sh
3. 重启hbase和opentsdb，一般会解决问题

完成上述操作重复第二步，如果依然无返回，可联系运维人员协助排查。



* 修订日期：20xx-xx-xx
* 处置时间：10分钟
* 操作步骤：

1. 登录容器云管理平台（xxx.xxx.xxx.xxx），如上图指示停止global应用
2. 容器云管理平台开启global应用
3. 验证方法：登陆global（xxx.xxx.xxx.xxx）平台，测试业务集群中创建服务正常



* 修订日期：20xx-xx-xx
* 处置时间：约10分钟
* 操作步骤：

1. 应用无法响应，去xxx.xxx.xxx.xxx查看容器云平台日志，确定问题。
2. 如果访问容器云平台系统能跳转到sso登录界面，sso系统无法登陆，找cbsp相关人员解决。
3. 如果登录容器云平台超级用户，无法登陆，没有提示，则可能系统出现故障，进行故障排查，进入核心平台xxx.xxx.xxx.xxx查看登录时间节点的日志，确定问题。如果没有日志，进入容器云平台安装主机10.1.132.103，使用docker ps | grep容器云平台查看容器状态，up为正常。然后使用docker logs容器云平台–since

“登录时间”查看是否有异常信息。

1. 如果为系统容器问题，可以更新容器云平台系统，如为网络问题，可以找网络负责人员解决，如为存储问题导致系统无法正常运行，建议优化存储清理停止容器及其他无用存储信息，再更新应用。



* 修订日期：20xx-xx-xx
* 处置时间：约30分钟
* 操作步骤：

1. 去console.cbjup.citicbank.com查看登录用户是否有权限操作应用。
2. 如有权限，无法提交，可刷新页面重试。
3. 反复刷新还不能提交，排除网络问题，查看日志,先去核心平台10.1.132.102日志里查看，核心平台点击容器，应用，然后点击容器云平台-service，找到日志查看，如没有日志，则进入容器云平台安装主机10.1.132.103，使用docker ps | grep容器云平台查看容器状态，up为正常。然后使用docker logs容器云平台 - since，查看错误根源。如果日志记录太多，找到困难，就直接docker logs容器云平台等待日志打印到当前状态，然后重新点击提交工单按钮，看当前打印日志，打印完日志立即ctrl+C退出，然后查看，因为容器云平台系统在不断刷新页面，接下来的日志会把当前日志覆盖掉。可能的原因：

* 如果日志里有”failedto10.2.1.99/itsm”判断为ITSM系统做变更，无法访问，等待ITSM能访问再重试，可解决。
* 如果日志里有“mysql”相关字样，判断为数据库容器问题或字段插入失败，进入核心平台查看mysql容器是否正常运行，如果有错误，重新启动容器，问题可解决。如果操作不了mysql容器，可在核心平台找到mysql安装主机，进入mysql安装机器，使用命令service mysql restart重新启动mysql。如果mysql运行正常，建议描述和标题不要太长，否则插入数据库可能失败。在容器云平台系统内查看工单执行时间，时间为空提交会失败，添加一个执行时间即可。如果日志里有NullPointerException,则判断执行数据有问题，例如未部署的应用直接提交更新，程序无法解析。遇到此问题，建议仔细检查数据，在容器云平台查看应用是否部署成功，应用下的服务是否在正常运行。



* 修订日期：20xx-xx-xx
* 处置时间：约30分钟
* 操作步骤：

1. 查看核心平台10.1.132.126:28090是否正常运行，检查核心平台主页资源显示情况，如果没有显示，确定是核心平台问题，找平台运维解决。
2. 检查是否网络问题，无法获取到数据，可测试网速。
3. 查看核心平台xxx.xxx.xxx.xxx关于容器云平台的运行日志，如没有日志，则进入容器云平台安装主机10.1.132.103，使用docker ps | grep容器云平台查看容器状态，up为正常。然后使用docker logs容器云平台 - since，查看错误根源。如果日志记录太多，找到困难，就直接docker logs容器云平台等待日志打印到当前状态，然后重新点击提交工单按钮，看当前打印日志，打印完日志立即ctrl+C退出，然后查看，因为容器云平台系统在不断刷新页面，接下来的日志会把当前日志覆盖掉。

如果日志里有”failedto10.1.132.109：20081”则为核心平台异常，如果核心平台运行正常，查看console.cbjup.citicbank.com配置参数里面的ACP\_Token和核心平台登录用户token是否一致，如不一致，需更改cbsp配置。

1. 如果是在核心平台查看容器云平台系统发现异常（状态为错误），可更新容器云平台系统，在核心平台点击应用进入容器云平台应用页，点击右上角更新按钮，确认即可。