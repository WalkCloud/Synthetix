ACP全栈云平台的服务网格管理功能将部署微服务治理平台组件的流程可视化。管理员只需配置主要参数，可以方便快捷的执行部署服务网格、修改服务网格参数等操作。

* 创建服务网格

管理员为每一个需要微服务治理的 Kubernetes 集群，部署包括Istio在内的所有微服务治理平台组件，并接入必要的外部组件，使集群具备容器化微服务治理的能力。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

创建Istio服务网格

* 集群管理

平台支持多主架构服务网格，为已创建的服务网格添加集群即可让网格纳管多个集群，多个集群的网格配置相同，但集群之间完全独立。多集群管理模式下的服务网格具备以下能力：

* 任一集群故障，不会影响到网格下其他集群的工作
* 微服务可以部署在网格下的任意集群中
* 微服务跨地域故障容灾。

平台支持在服务网格界面对集群添加、删除的操作，支持统一更新网格纳管的集群上的网格配置，例如：Sidecar 资源配额、调用链采样率等。

![电脑软件截图  描述已自动生成](data:image/png;base64...)

服务网格管理

* 管理网关

服务网关包括入口网关和出口网关，用于管理服务网格的入站和出站流量，让用户可以指定允许进入或离开网格的流量。创建服务网格或为网格添加集群时，会为网格纳管的所有集群部署内置的入口网关和出口网关，但内置的网关仅提供了一组 HTTP/HTTPS 端口供业务系统接入，无法满足更加丰富的、特定的业务需求。

用户可通过在集群中部署新的网关满足更加丰富的入站和出站流量管理需求：

* 网格下业务划分不同的区域，业务区域之间网络完全隔离。
* 让特定的业务独占网关，确保通过网关访问的性能、安全。
* 让大流量业务系统独占端口，确保不会因为业务系统的请求流量过大造成端口阻塞，导致通过网关访问其他业务系统时访问异常。

![图形用户界面, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

网关管理

* 黑白名单

通过为服务网格下集群中部署的内置入口网关设置黑名单或白名单，可基于 IP 实现网格外服务对集群内服务的访问权限控制。

黑白名单功能作用于当前服务网格的单个集群，对Istio-ingressgateway接收的请求进行访问控制。对于七层负载均衡器转发的请求，需将服务源IP加到x-forwarded-for中，以便Istio-ingressgateway能够获取。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

黑白名单管理

* 网络监控

平台嵌入了Istio Grafana仪表板来监控Istio的整体运行状态。服务网格正常运行时方可查看。

![电脑萤幕的截图  描述已自动生成](data:image/png;base64...)

网络监控界面

* 外部服务

当外部服务所在区域和当前服务网格网络互通时，服务网格内服务可直接访问外部服务；当外部服务所在区域和当前服务网格网络互相隔离时，可通过在网格纳管的集群下添加外部服务，将外部服务注册到集群内，即可确保集群内服务在不影响开发体验的情况下访问外部服务，并对其进行流量监控。

在集群下添加外部服务时，可将其绑定至集群上部署的出口网关的指定端口，并通过配置出口网关的路由规则，使集群内的对外访问流量从出口网关发出，经过外部服务所在区域的入口网关转发至外部服务。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

外部服务管理



ACP全栈云平台的微服务治理功能是基于开源组件 Istio 实现的，为了在 Istio 的基础上扩展平台的微服务治理能力，实现更为丰富的定制化服务治理需求，平台推出了 WASM 插件管理功能。

通过 WASM 插件可以把扩展的 Envoy Filter（WASM 提供）部署到服务网格纳管的集群下相应的 Envoy 中。通过这种过滤器扩展机制，可以轻松扩展 Envoy 的功能并为其在服务网格中的应用提供更为丰富的服务治理体验。例如，当您的应用希望通过 dubbo、soap、rmi 等协议进行互相调用，或是需要为您的应用定制服务治理能力时，均可通过 WASM 插件实现。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

WASM插件管理界面

通过创建插件，在平台上添加一个 WASM 插件，类似于创建了一个模板，该模板中保存了部署 WASM 应用所需的镜像、支持的协议、EnvoyFilter 模板（定义了要编译进 Envoy 的 filter）等。创建成功后，可基于模板快速将插件安装至平台上的服务网格，便于插件的分发、部署和管理。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

创建WASM插件

平台支持可视化管理插件，将已创建的插件安装至平台上已有的服务网格，即可将 WASM 插件中的提供的 filter 编译进网格纳管的集群下的 Envoy 中，从而将 WASM 的扩展能力分发给网格纳管的业务集群。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

WASM插件管理



ACP全栈云平台的安全管理功能基于容器运行时安全工具 Falco 实现，会针对安全规则库中的规则对系统调用行为进行监控，当容器发生存在安全隐患的行为时则触发告警，并可自动向安全负责人发送安全规则告警消息，以便相关人员及时排除安全隐患、修复安全漏洞，确保系统的容器运行时安全。



在安全面板中，可查看平台上所有集群在指定时间范围内，发生安全规则告警的统计数据、告警在集群中的分布情况，以及相应的告警历史记录。

方便安全负责人根据统计数据全面了解并分析集群的容器安全程度，加强安全管理。并可结合详细的告警历史的事件说明信息快速定位并解决安全问题。

![图形用户界面, 文本, 应用程序  描述已自动生成](data:image/png;base64...)

安全面板



安全规则库包括了指定集群下的所有内置规则和自定义规则。规则中定义了匹配存在安全隐患的事件（内核调用行为）的条件，当规则匹配到相应的事件时，自动触发安全规则告警。

* 内置规则：Falco 基于其强大的规则引擎提供的一组默认规则，覆盖了常见的安全检测场景。平台上部署了安全组件的集群，会默认生成并启用这些规则，这些内置规则将作用于集群下的所有容器。
* 规则模板：规则模板是平台根据 Falco 提供的默认规则生成的标准化资源，每个默认规则都有一个模板与之对应。
* 自定义规则：为了方便用户自定义规则的作用范围，平台支持您基于相应的规则模板创建自定义安全规则，且可设置规则的作用范围。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

安全规则库

平台的内置规则针对集群下所有的容器，不支持设置规则的作用范围。

平台将 Falco 支持的默认规则抽象成了规则模板，基于模板在集群下创建自定义规则时，可设置规则的作用范围。如需限定规则的作用范围，可禁用内置规则，创建并启用设置了作用范围的自定义规则。

![图形用户界面, 应用程序, Teams  描述已自动生成](data:image/png;base64...)

创建规则库



平台提供可视化管理界面，支持为当前集群创建、更新或删除容器组策略，包含基本策略、主机路径策略、主机端口策略、用户运行策略等部分。

![图形用户界面, 文本, 应用程序, 电子邮件  描述已自动生成](data:image/png;base64...)

容器组安全策略